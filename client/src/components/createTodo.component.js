import React, { Component } from 'react';
import Form from './createTodoForm.component';

export default class CreateTodo extends Component{
    render(){
        return(
            <Form />
        );
    };
}